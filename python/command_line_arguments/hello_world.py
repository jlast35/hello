#!/usr/bin/env python
print "Hello World"
#echo "hello world"

#if no #! line is included and the file is run as a command, ($ hello_world.py) instead of ($ python hello_world.py)
#Cygwin assumes the file is a bash script and tries to execute it as such